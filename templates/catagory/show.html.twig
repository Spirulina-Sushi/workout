{% extends 'base.html.twig' %}

{% block title %}Catagory{% endblock %}

{% block body %}
    <h1>Catagory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catagory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ catagory.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('catagory_index') }}">back to list</a>

    <a href="{{ path('catagory_edit', {'id': catagory.id}) }}">edit</a>

    {{ include('catagory/_delete_form.html.twig') }}
{% endblock %}
