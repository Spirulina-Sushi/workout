{% extends 'base.html.twig' %}

{% block title %}BodyPart{% endblock %}

{% block body %}
    <h1>BodyPart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ body_part.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ body_part.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('body_part_index') }}">back to list</a>

    <a href="{{ path('body_part_edit', {'id': body_part.id}) }}">edit</a>

    {{ include('body_part/_delete_form.html.twig') }}
{% endblock %}
